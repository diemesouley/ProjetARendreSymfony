{% extends 'base.html.twig' %}

{% block title %}Frais{% endblock %}

{% block body %}
    <h1>Frais</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ frai.id }}</td>
            </tr>
            <tr>
                <th>Min</th>
                <td>{{ frai.min }}</td>
            </tr>
            <tr>
                <th>Max</th>
                <td>{{ frai.max }}</td>
            </tr>
            <tr>
                <th>Frais</th>
                <td>{{ frai.frais }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('frais_index') }}">back to list</a>

    <a href="{{ path('frais_edit', {'id': frai.id}) }}">edit</a>

    {{ include('frais/_delete_form.html.twig') }}
{% endblock %}
